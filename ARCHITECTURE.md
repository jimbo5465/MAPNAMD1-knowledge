# معماری ربات دانش سازمانی MAPNAMD1-knowledge

## نمای کلی

ربات روی **دو پلتفرم** (تلگرام و بله) اجرا می‌شود که هر دو به **یک دیتابیس مشترک** وصل‌اند.
هویت کاربر بین دو پلتفرم با تطبیق شمارهٔ موبایل نرمال‌شده + کد پرسنلی **لینک** می‌شود.

```
کاربر تلگرام                کاربر بله
      │                         │
      ▼                         ▼
main.py (telegram)        main_bale.py (bale)
      │                         │
      ▼                         ▼
handlers/                 bale_app/ (فریم‌ورک سبک سفارشی)
       \                       /
        \                     /
         ▼                   ▼
         engine (AI) ──► db/ (مشترک)
                          │
                          ▼
                  data/knowledge.db (SQLite واحد)
```

## لایه‌ها

### ۱. main.py / main_bale.py — نقاط ورود

هر فایل مسئول یک پلتفرم است:
- راه‌اندازی logging
- مقداردهی اولیه‌ی دیتابیس (ساخت جداول + migration)
- ساخت Application (python-telegram-bot یا فریم‌ورک داخلی بله)
- ثبت همه‌ی handler ها (ConversationHandler + CommandHandler + CallbackQueryHandler)
- ثبت هندلر سراسری قفل «در حال پردازش» (busy guard)
- ثبت error handler سراسری
- شروع polling

**نباید** شامل SQL، منطق AI، تولید خروجی یا منطق کسب‌وکار باشد.

### ۲. handlers/ و bale_app/ — لایه‌های رابط کاربر

`handlers/` برای تلگرام و `bale_app/` برای بله. منطق یکسان است؛ فقط منبع import فرق می‌کند.

مدیریت ارتباط با کاربر:
- دریافت پیام/ویس/عکس
- مدیریت Conversation (state machine)
- نمایش keyboard
- اعتبارسنجی اولیه‌ی ورودی
- فراخوانی engine یا db

| فایل (در هر دو پوشه) | وظیفه |
|---|---|
| `registration.py` | ثبت‌نام کاربر + **لینک خودکار حساب دو پلتفرم**، پروفایل و ویرایش |
| `observations.py` | ثبت مشاهده (متن/ویس/عکس + عنوان + هشتگ + تاریخ + پیوست)، مرور، جستجو |
| `knowledge.py` | ثبت دانش (دستی + مصاحبه با AI + عکس)، ساخت پیش‌نویس DANA |
| `auth.py` | `require_registration` دکوراتور — اجباری بودن ثبت‌نام |
| `keyboards.py` | سازنده‌های InlineKeyboardMarkup |
| `jalali_calendar.py` | تقویم جلالی تعاملی برای انتخاب تاریخ |

فایل‌های خاص یک پلتفرم:

| فایل | پلتفرم | وظیفه |
|---|---|---|
| `bale_app/framework.py` | بله | Update/Context/Bot/Dispatcher سبک — شبیه‌سازی API تلگرام روی بله |
| `handlers/prompt_cleanup.py` | تلگرام | حذف پرامپت قبلی ربات در گام‌های گفتگو |

**نکته:** handler ها مجاز به اجرای مستقیم SQL نیستند — فقط از طریق `db/models.py`.

### ۳. engine/ — لایه‌ی منطق کسب‌وکار

هیچ وابستگی به تلگرام/بله ندارد — فقط ورودی/خروجی純 Python.

| فایل | وظیفه |
|---|---|
| `knowledge_ai.py` | استخراج فیلدهای ساختارمند از متن با AI (OpenCode Go / deepseek-v4-flash) |
| `knowledge_interview.py` | مصاحبه‌ی هوشمند (سؤال‌های متنی بر اساس نوع دانش)، ساخت فرم نهایی DANA، پیشنهاد مسیر درخت |
| `knowledge_draft.py` | ساخت گزارش DANA از فیلدهای استخراج‌شده |
| `knowledge_render.py` | خروجی PDF (reportlab) و Word (python-docx) با پشتیبانی فارسی |
| `knowledge_tree.py` | درخت دانش سازمانی — مسیرهای سلسله‌مراتبی |
| `knowledge_numbering.py` | شماره‌گذاری خودکار دانش |

### ۴. db/ — لایه‌ی دسترسی به داده (مشترک دو پلتفرم)

تنها لایه‌ای که مجاز به اجرای SQL است. تمام CRUD از طریق `db/models.py`.

| فایل | وظیفه |
|---|---|
| `init.py` | `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE` migration (شامل bale_id/phone_norm) |
| `models.py` | توابع CRUD + **لینک حساب‌های دو پلتفرم** (`register_or_link_user`) |
| `phone_utils.py` | نرمال‌سازی شمارهٔ موبایل — کلید مشترک ۱۰ رقمی |

**جداول اصلی:**
- `users` — کاربران (ستون‌های `telegram_id`, `bale_id`, `phone_norm` برای لینک دوپلتفرمی)
- `knowledge_entries` — دانش‌های ثبت‌شده
- `knowledge_photos` — عکس‌های دانش
- `observations` — مشاهدات میدانی
- `observation_attachments` — پیوست‌های مشاهده (عکس/PDF/فایل)

#### مدل هویت دوپلتفرمی

```
users
├── telegram_id  ← شناسه تلگرام (یا شناسه پلتفرم اولِ ثبت‌نام)
├── bale_id      ← شناسه بله (nullable, UNIQUE)
└── phone_norm   ← کلید تطبیق: ۱۰ رقم محلی شماره (مثل 9155107315)
```

- `get_user_by_platform_id(id)` — کاربر را با **هر کدام** از دو ستون پیدا می‌کند؛ تمام
  توابع db (مشاهدات، پیش‌نویس، پروفایل) ورودی «شناسهٔ پلتفرم» می‌گیرند و داخلی resolve می‌کنند.
- `register_or_link_user(platform=..., platform_id=..., phone=..., personnel_code=...)` —
  در پایان ثبت‌نام صدا زده می‌شود؛ اگر رکوردی با همان `phone_norm` + `personnel_code`
  موجود باشد، حساب فعلی به آن لینک و رکوردهای تکراری قدیمی ادغام می‌شوند.
- **لایهٔ امنیتی:** لینک فقط با تطبیق همزمان شماره + کد پرسنلی انجام می‌شود —
  دانستن شمارهٔ شخص به‌تنهایی دسترسی به داده‌هایش نمی‌دهد.

### ۵. utils/ — ابزارهای عمومی

| فایل | وظیفه |
|---|---|
| `busy_lock.py` | قفل per-user «در حال پردازش» با انقضای خودکار (۹۰ ثانیه) |
| `dates.py` | تبدیل تاریخ جلالی ↔ میلادی، اعتبارسنجی، نمایش فارسی |
| `jalali_calendar_core.py` | هستهٔ محاسبات تقویم تعاملی |
| `validators.py` | اعتبارسنجی ورودی |

## اصول معماری

1. **لایه‌بندی:** handler → engine → db (هر لایه فقط لایه‌ی پایین‌تر را صدا می‌زند)
2. **دسترسی به DB:** فقط از طریق `db/models.py` — هیچ handler ای مجاز به SQL مستقیم نیست
3. **قفل AI:** هنگام پردازش AI، کاربر پیام «⏳ در حال بررسی...» می‌گیرد — با `utils/busy_lock.py`
4. **ثبت‌نام اجباری:** کاربر قبل از هر کاری باید ثبت‌نام کند (دکوراتور `require_registration`)
5. **migration خودکار:** ستون‌های جدید با `ALTER TABLE` در `init_db()` اضافه می‌شوند
6. **ذخیره‌سازی عکس:** سایز متوسط (~۸۰۰px) برای صرفه‌جویی در فضا
7. **systemd:** سرویس با `Restart=always` و `MemoryMax`/`CPUQuota` — خروج تمیز
8. **یک دیتابیس، دو پلتفرم:** هویت واحد کاربر در بله و تلگرام — داده‌ها کاملاً مشترک‌اند
